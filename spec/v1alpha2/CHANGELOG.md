# Changelog — `v1alpha2`

## Unreleased

- Copy of `v1alpha1` as `v1alpha2` (versioned paths updated).
- Daemon schema and OpenAPI updates (instance list response, runtime instance fields, async run submission).
