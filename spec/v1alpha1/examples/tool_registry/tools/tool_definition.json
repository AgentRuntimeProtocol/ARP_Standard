{
  "tool_id": "tool_echo",
  "name": "echo",
  "description": "Echoes input back to the caller.",
  "input_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["text"],
    "properties": {
      "text": { "type": "string" }
    }
  },
  "output_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["text"],
    "properties": {
      "text": { "type": "string" }
    }
  },
  "source": "registry_local"
}

