# Conformance — `v1alpha1`

This directory defines what is required vs optional for implementations, and provides golden JSON vectors.

## Golden vectors

- `json_vectors/` contains known-good request/response payloads that must validate against the JSON Schemas.

## Rules

- `rules/` contains the conformance requirements for endpoints and payloads.

